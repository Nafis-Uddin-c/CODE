#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a, b;
    cin>>a>>b;

    // int domino = 0;

    // while((a*b) >= domino*2)
    // {
    //     domino++;
    // }

    // cout<<domino -1;
    
    int ans = (a*b)/2;
    cout<<ans;

    return 0;
}